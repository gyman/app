{% extends 'MembersBundle::layout.html.twig' %}

{% block content %}
<div class="contentwrapper">
    <div class="heading">
        <h3>Edytuj użytkownika</h3>
    </div>

    <div class="span12">
    {% include "::_flash.html.twig" %}

    {{ form_start(form, {
        attr : {
            'id' : "memberForm",
            'class' : "form-horizontal row-fluid",
            'novalidate' : 'novalidate'
        },
        action : path('gyman_member_edit', {"id" : member.id})
    }) }}

    {%  include "@Members/Default/member_form_inner.html.twig" %}

    <div class="row-fluid">
        <div class="span12">
            <button class="btn btn-large btn-primary pull-right" type="submit">
                Zapisz
            </button>
        </div>
    </div>

    {{ form_end(form) }}

    </div>
</div>
{% endblock content %}
